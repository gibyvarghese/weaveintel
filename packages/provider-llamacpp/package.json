{
  "name": "@weaveintel/provider-llamacpp",
  "version": "0.0.1",
  "description": "llama.cpp HTTP server provider for weaveIntel — local GGUF models via OpenAI-compatible API",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@weaveintel/core": "*",
    "@weaveintel/models": "*",
    "@weaveintel/tool-schema": "*"
  },
  "license": "MIT"
}
