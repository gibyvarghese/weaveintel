{
  "name": "@weaveintel/oauth",
  "version": "0.1.0",
  "description": "OAuth 2.0 provider integration (Google, GitHub, Microsoft, Apple, Facebook)",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "!dist/**/*.test.*",
    "!dist/**/*.spec.*"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@weaveintel/core": "^0.0.1"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^3.2.6",
    "@weaveintel/persistence": "^0.0.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
